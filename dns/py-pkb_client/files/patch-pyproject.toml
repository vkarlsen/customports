--- pyproject.toml.orig	2026-03-15 20:21:12 UTC
+++ pyproject.toml
@@ -1,3 +1,8 @@
+[project]
+name = "pkb_client"
+version = "2.3.1"
 [build-system]
 requires = ["setuptools>=50.3.0", "wheel"]
 build-backend = "setuptools.build_meta:__legacy__"
+[tool.setuptools.packages.find]
+exclude = ["tests", "tests.*", "docs", "docs.*"]
